{
  "name": "swivik",
  "private": true,
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
